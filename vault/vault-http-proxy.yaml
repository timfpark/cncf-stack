apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
    name: vault-proxy
    namespace: vault
spec:
    virtualhost:
        fqdn: vault.nublada.cloud
        tls:
            secretName: nublada-cloud-tls
    routes:
        - services:
              - name: vault-active
                port: 8200
